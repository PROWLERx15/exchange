[project]
name = "exchange"
version = "0.1.0"
description = "An electronic exchange with a central limit order book and matching engine."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.16.6",
]
